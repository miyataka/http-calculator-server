services:
  dev:
    build: .
    image: http-calculator-server-dev
    # ホストと同じパスにマウントすると、コンテナで生成した
    # compile_commands.json をホスト側の clangd がそのまま使える
    volumes:
      - ${PWD}:${PWD}
    working_dir: ${PWD}
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true
    command: bash
